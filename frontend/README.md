# Diabetic Retinopathy Detection - React Frontend

## Setup

1. Install Node.js (version 14 or higher)

2. Install dependencies:
```bash
npm install
```

3. Start the development server:
```bash
npm start
```

The application will open at `http://localhost:3000`

## Build for Production

```bash
npm run build
```

This creates an optimized production build in the `build` folder.

## Features

- 🎨 Modern, responsive UI with Bootstrap 5
- 📤 Drag & drop image upload
- 🧠 Real-time AI predictions
- 📊 Detailed probability visualization
- 📱 Mobile-friendly design
- ⚡ Fast and intuitive user experience

## Configuration

The frontend connects to the Flask backend at `http://localhost:5000` by default.

To change the API URL, set the `REACT_APP_API_URL` environment variable.
